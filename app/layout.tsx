import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "WeddingTips.ID - Merencanakan Pernikahan dengan Lebih Tenang dan Terarah",
  description: "Platform perencanaan pernikahan yang membantu calon pengantin di Jabodetabek merencanakan pernikahan dengan cara yang lebih rapi, realistis, dan nyaman. Didampingi AI dan konsultan profesional.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
